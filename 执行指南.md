# 🚀 小红书AI发布助手 - 执行指南

## 📋 目录
- [系统要求](#系统要求)
- [安装依赖](#安装依赖)
- [执行方法](#执行方法)
- [常见问题](#常见问题)

---

## 系统要求

### 最低要求
- **Python**: 3.8 或更高版本
- **内存**: 4GB RAM (推荐 8GB+)
- **磁盘空间**: 2GB 可用空间
- **操作系统**: Windows 10/11, macOS 10.15+, Linux (Ubuntu 18.04+)

### 推荐配置
- **Python**: 3.10 或更高版本
- **浏览器**: Chrome/Chromium (自动安装)
- **网络**: 稳定的网络连接

---

## 安装依赖

### 方法一：自动安装（推荐）

#### Windows
```bash
# 双击运行安装脚本
install.bat

# 或使用命令行
python deploy.py
```

#### macOS
```bash
# 运行 macOS 专用安装脚本
./install_mac.sh

# 或使用通用部署脚本
python3 deploy.py
```

#### Linux
```bash
# 运行 Linux 安装脚本
./install.sh

# 或使用通用部署脚本
python3 deploy.py
```

### 方法二：手动安装

```bash
# 1. 创建虚拟环境（推荐）
python -m venv venv

# 2. 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 安装 Playwright 浏览器
playwright install chromium

# 5. 初始化数据库（可选，首次运行会自动创建）
python init_db.py
```

### 方法三：验证安装

运行验证脚本检查系统配置：

```bash
python verify_installation.py
```

这个脚本会检查：
- ✅ Python 版本
- ✅ 依赖包安装状态
- ✅ GUI 显示环境
- ✅ Web 界面可用性
- ✅ 数据库状态

---

## 执行方法

本项目提供 **两种界面** 供您选择：

### 🖥️ 方法一：PyQt5 桌面GUI界面（推荐日常使用）

**适用场景**：
- ✅ 个人电脑使用
- ✅ 需要可视化界面
- ✅ Windows/macOS/Linux 桌面环境

**启动方式**：

#### Windows
```bash
# 方式1：双击启动脚本
启动程序.bat

# 方式2：命令行启动
python main.py

# 方式3：使用虚拟环境
venv\Scripts\python.exe main.py
```

#### macOS/Linux
```bash
# 方式1：使用启动脚本
./启动程序.sh

# 方式2：命令行启动
python3 main.py

# 方式3：使用虚拟环境
source venv/bin/activate
python main.py
```

**界面功能**：
- 🏠 **主页**: AI内容生成和一键发布
- 👤 **用户管理**: 多账户管理和配置
- 🧰 **工具箱**: 视频处理和数据库管理
- ⚙️ **设置**: 应用程序配置

### 🌐 方法二：FastAPI Web界面（适合服务器部署）

**适用场景**：
- ✅ 服务器部署
- ✅ 无图形界面环境
- ✅ 远程访问
- ✅ 团队协作

**启动方式**：

```bash
# 启动 Web 服务器
python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000

# 或使用开发模式（自动重载）
python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000 --reload

# 后台运行（Linux/macOS）
nohup python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000 > web.log 2>&1 &
```

**访问方式**：
- 本地访问: http://localhost:8000
- 远程访问: http://服务器IP:8000

**Web API 端点**：
- `GET /`: 主页面
- `POST /api/login`: 登录小红书
- `POST /api/content`: 创建内容
- `POST /api/publish`: 发布内容
- `GET /api/status`: 检查登录状态

---

## 常见问题

### ❓ 问题1：界面无法启动

**症状**：运行 `python main.py` 后无反应或报错

**解决方案**：

1. **检查 Python 版本**
   ```bash
   python --version
   # 确保 >= 3.8
   ```

2. **检查依赖安装**
   ```bash
   python verify_installation.py
   ```

3. **重新安装 PyQt5**（如果 GUI 界面问题）
   ```bash
   pip uninstall PyQt5 PyQt5-Qt5 PyQt5-sip
   pip install PyQt5>=5.15.0
   ```

4. **使用 Web 界面作为替代**
   ```bash
   python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000
   ```

### ❓ 问题2：显示 "No module named 'XXX'"

**原因**：依赖包未安装或虚拟环境未激活

**解决方案**：
```bash
# 重新安装所有依赖
pip install -r requirements.txt

# 或安装单个缺失的包
pip install 包名
```

### ❓ 问题3：PyQt5 显示 "could not load the Qt platform plugin"

**原因**：缺少图形环境或系统库

**解决方案**：

**Linux**:
```bash
# Ubuntu/Debian
sudo apt-get install libgl1-mesa-glx libxkbcommon-x11-0 libxcb-xinerama0

# Fedora/RHEL
sudo dnf install mesa-libGL libxkbcommon-x11

# 或使用 Web 界面
python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000
```

**无头服务器**（无显示环境）：
```bash
# 仅使用 Web 界面
python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000
```

### ❓ 问题4：浏览器自动化失败

**症状**：登录或发布时浏览器无法打开

**解决方案**：

1. **安装 Playwright 浏览器**
   ```bash
   playwright install chromium
   ```

2. **检查网络连接**
   ```bash
   # 确保可以访问外网下载浏览器
   ```

3. **手动指定浏览器路径**（高级）
   ```python
   # 在 src/config/config.py 中配置
   ```

### ❓ 问题5：数据库错误

**症状**：启动时提示数据库相关错误

**解决方案**：

1. **初始化数据库**
   ```bash
   python init_db.py
   ```

2. **删除并重建数据库**
   ```bash
   # 备份重要数据后执行
   rm ~/.xhs_system/xhs_data.db
   python init_db.py
   ```

3. **检查数据库路径权限**
   ```bash
   ls -la ~/.xhs_system/
   # 确保有读写权限
   ```

### ❓ 问题6：如何在无界面服务器上运行？

**解决方案**：使用 Web 界面

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动 Web 服务
python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000

# 3. 在浏览器访问
# http://服务器IP:8000
```

**使用 systemd 设置自动启动**（Linux）:

创建服务文件 `/etc/systemd/system/xhs-publisher.service`:
```ini
[Unit]
Description=XHS AI Publisher Web Service
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/xhs_ai_publisher
ExecStart=/path/to/venv/bin/python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8000
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl enable xhs-publisher
sudo systemctl start xhs-publisher
sudo systemctl status xhs-publisher
```

### ❓ 问题7：端口被占用

**症状**：启动 Web 界面时提示端口 8000 被占用

**解决方案**：

1. **使用不同端口**
   ```bash
   python -m uvicorn src.web.app:app --host 0.0.0.0 --port 8080
   ```

2. **查找并关闭占用端口的进程**
   ```bash
   # Linux/macOS
   lsof -i :8000
   kill -9 <PID>
   
   # Windows
   netstat -ano | findstr :8000
   taskkill /PID <PID> /F
   ```

---

## 📊 性能优化建议

### 提升运行速度
1. **使用虚拟环境**：隔离依赖，避免冲突
2. **关闭不必要的应用**：释放内存
3. **使用 SSD**：提升数据库和文件访问速度

### 降低资源占用
1. **无头模式**：使用 Web 界面而非 GUI
2. **限制并发**：避免同时运行多个浏览器实例
3. **定期清理**：删除旧的日志和临时文件

---

## 🎯 快速开始检查清单

开始使用前，确保完成以下步骤：

- [ ] Python 版本 >= 3.8
- [ ] 运行 `pip install -r requirements.txt`
- [ ] 运行 `playwright install chromium`
- [ ] 运行 `python verify_installation.py` 验证
- [ ] 选择合适的界面方式（GUI 或 Web）
- [ ] 启动应用并测试

---

## 📞 获取帮助

如果以上方法都无法解决您的问题：

1. **查看日志文件**：
   - GUI 界面日志：`~/Desktop/xhsai_error.log`
   - 应用日志：`~/.xhs_system/logs/`

2. **运行诊断脚本**：
   ```bash
   python verify_installation.py
   ```

3. **提交 Issue**：
   - GitHub: https://github.com/BetaStreetOmnis/xhs_ai_publisher/issues
   - 请附上：
     - 错误信息
     - 系统环境（OS、Python版本）
     - 执行步骤

---

## ✅ 验证成功标志

当您看到以下任一情况，说明已成功运行：

### GUI 界面成功
```
✅ 数据库已准备就绪
✅ 显示主窗口
✅ 侧边栏显示：🏠 👤 🧰 ⚙️
```

### Web 界面成功
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

---

<div align="center">

**🎉 现在您可以开始使用小红书AI发布助手了！**

如有疑问，请查看 [README.md](readme.md) 或提交 Issue

</div>
